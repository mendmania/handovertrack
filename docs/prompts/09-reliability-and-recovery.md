# HandoverTrack — Task 09: whole-trial reliability and recovery

**Prepared only. Do not start without an explicit Task 09 assignment.**
Read README, repository instructions, implementation status, Tasks 03–08 handoffs,
the current Task 05 checkpoint, infra/kubernetes/RECOVERY.md and the Task 08 PR/CI results. Inspect
branch/main and preserve all source/configuration, original photos, accepted receipts,
SQLite v5 answer/media queues and conflict payloads, immutable reports, shares,
decisions, previous backups and retained recovery rehearsals.

The owner merged PR #7 (Tasks 06–08) on 2026-09-24; verified merge commit:
`6ff5f38314621247ab7b082dfa3030fc9f704e96`. At prompt preparation, main's
Validate and publish HandoverTrack run `35972037481` was in progress. Recheck
the actual current main and workflow results at execution time. Merged code and
published images do not establish a live deployment or close native/recovery gates.

When explicitly assigned, perform this reliability work, repair demonstrated
defects, commit, push and open a new Task 09 PR targeting main. The owner's standing
publication request applies; do not stop at proposed commands. This assignment
does not authorize a new merge, live deployment, automation activation or registry
visibility change. Independent local work can proceed while device/owner-dependent
checks remain unavailable. Keep the trial disposable-only until its gates pass.

## Start from merged main and establish evidence

1. Inspect the working tree, current branch, repository instructions and remote.
   Fetch current main and create `codex/reliability-and-recovery` from it, or resume
   an existing matching Task 09 branch after inspecting its contents. PR #7 is
   merged; do not continue publishing Task 09 to its old branch/PR. Preserve and
   carry this prepared prompt and any other relevant local changes safely. No
   reset, force push, clean, database reseed or unrelated changes.
2. Record source SHA, schema versions, test fixtures, software/device versions,
   owned services, retained data and current release evidence. Inspect workflows
   and release tooling before publishing. Do not claim runtime/image identity from
   source history alone or weaken schema/compatibility release guards.
3. Build a small acceptance matrix: scenario, previous evidence, environment,
   remaining action, actual result and blocker. Separate local/simulator/physical
   phone/live/independent recovery evidence. Attempt required available checks;
   ask early for device availability or missing operational decisions while doing
   independent work. Do not repeat historical questions already answered or treat
   an instruction to continue as confirmation of backup custody/responsibility.

## Reliability work

- Review the actual remaining native gates. Preserve Task 07's passing isolated
  answer offline-save/cold-restart/conflict/resolution evidence. Do not repeat passed
  checks without a concrete changed-code or unresolved-risk reason. Additional native
  photo dependency/account/org/tombstone retention UI and Task 05 physical upload
  interruption/restart/lost response remain open. Use retained isolated/test-device
  data; do not replace the existing HTTPS phone or its SQLite v4 originals.
  The retained schema-v4 Task 05 fault build and current schema-v5 client are
  different validation tracks. Inspect their compatibility before use. Prefer an
  isolated device/simulator for current Task 06–08 UI. Follow the scoped Task 05
  procedure only when the phone is available and the required owner interaction
  is confirmed; do not silently install v5 over the retained live v4 installation.
  Preserve pending intent, IDs, receipts and bytes; restore compatible normal
  operation after each controlled case. Mark unavailable physical checks NOT RUN.
- Validate the complete local path: accepted same-project evidence → frozen checklist
  completion → versioned composition → frozen PDF job → immutable publication →
  expiring/revocable exact-revision sharing → customer decision → manager review.
  Exercise process/database/response interruptions at meaningful transaction and
  lease boundaries. Retain original pending intent and stable idempotency keys.
  Prove recovery from committed-but-unacknowledged operations, expired leases and
  partial publication with one logical accepted result and intact audit history.
- Check combined upload/image/report/API resource budgets, bounded queues, PDF child
  deadlines, shutdown/drain, guest PDF concurrency and durable rate limits. Review
  the conservative shared-BFF-IP guest budget. Measure capacity; do not invent limits
  or claim production readiness from a small synthetic run.
  Include disk/inode-pressure intake rejection and recovery, pending-intent
  preservation, queue age/worker heartbeat/backup-age checks and failure visibility.
  Inject faults only into owned isolated resources; never fill the shared host,
  kill PostgreSQL on the live cluster or alter Rrugë. Record measured workload,
  duration, latency, memory/disk observations and unavailable counters. Validate
  local failure signals without sending messages or enabling external alerts.
- Inspect guest and manager cache isolation, secrets in URLs/logs/telemetry/proxies,
  expiry/revocation under load and any edge range/conditional/cache behavior. Verify
  historical customer decisions never become acceptance of newer revisions. Typed
  guest names remain unverified claims. Do not introduce an identity/signature claim.
  Repair findings within the existing architecture and add regression coverage for
  demonstrated failure modes. Preserve TanStack Query on web/mobile as disposable
  cache and SQLite/app-owned files as authoritative offline state. No broad rewrite.

## Recovery and compatibility

Use isolated copies and additive migrations only. Existing operator scripts target
the live namespace and can stop writers: inspect before running, and add explicit
isolated-target guards or a bounded local rehearsal harness where needed. Do not
run live backup/restore commands just because they appear in a runbook. Validate
recovery coverage of SQL
001–007, SQLite v5, immutable originals and derivatives, all report snapshots/jobs/
artifacts, PDF/partial paths, six sharing/decision/rate tables, command/audit receipts
and ownership. Preserve all prior backups and rehearsal resources. Restored revoked
or expired capabilities must stay unavailable; decisions and PDF hashes must match.
No raw share secret should be recoverable from database backups. Keep renderer/font
versions required by unfinished snapshots and published artifacts.

Take and restore a new consistent backup of representative current-schema test
data in local isolated containers/volumes; do not allocate new cluster resources
for this assignment. Use the actual backup/restore path being claimed. Quiesce all relevant
isolated writers or demonstrate another consistent database/filesystem boundary.
Restore into fresh isolated data paths/DBs, with no public ingress or source
credentials that could reach live services. Verify the saved schema checksums,
table contents/ownership, auth behavior, all original/derivative/PDF hashes, frozen
snapshots, command/audit receipts and pending/terminal jobs. Verify pending native
queues separately from a consistent SQLite/files copy; server backup cannot cover
unsynced phone-only evidence. Test resumed jobs without overwriting immutable
outputs. Retain failed attempts and successful evidence; stop rehearsal controllers
afterward without deleting prior backups, volumes or resources.

Test both revocations already present in the backup and revocations/decisions
committed after its recovery point. An older dump cannot contain later changes.
Keep guest serving disabled on a restored copy until an explicit fail-closed
reconciliation or share-invalidation procedure prevents resurrecting revoked
capabilities. Never infer that a missing post-backup decision was never made or
silently permit a conflicting new decision; record the known gap and required
operator reconciliation before reopening decision writes. Do not mutate historical
backup files to make a test pass. Record backup timestamp, known data-loss window,
restore duration and exclusions rather than inventing recovery guarantees.
Apply the same recovery-point review to sessions and membership/permission
revocations; an older auth snapshot must not silently restore public authority.

Task 05's technical restore success does not prove independent access after Mac loss
or dependable operations. Record independent backup/key access, designated operator,
accepted cadence/availability and missed/failed-backup response only from actual
owner decisions/evidence. Do not ask for secret values or invent approval. If these
remain unavailable, mark NOT RUN/unconfirmed and continue independent local work.
Do not activate unattended backup/deployment automation without explicit authorization.
Ask only for nonsecret facts: the independently accessible backup destination,
recovery-key access method after loss of the Mac, responsible operator, cadence,
availability and failed/missed-run response. The owner must select any new external
destination; do not buy storage or reuse Rrugë credentials. A same-Mac rehearsal
can pass technical restoration while independent disaster recovery remains NOT RUN.

Validate old-client/new-feed compatibility deliberately: the existing phone cannot
consume Task 06 checklist entities and must not be silently migrated/downgraded over
pending work. Any future release requires a separately authorized coordinated API,
web, worker/native and database plan, target filesystem/resource validation and
current backup/application restore. Do not use old 001–004 release assumptions.
Prepare a concrete compatible release/rollback runbook with exact migration and
client ordering, preflight gates, writer drain, verification and stopping points.
Exercise it on isolated copies where possible. Do not bypass the current release
tooling's schema-contract rejection to make 005–007 deploy automatically. Application
rollback is valid only with compatible schema/data/client behavior; never downgrade
SQLite or restore an old database over newer accepted evidence. Live rehearsal or
release remains outside this assignment and must be reported as NOT RUN.

## Deliverables and boundaries

Run meaningful automated and interactive regressions; record PASS/FAIL/NOT RUN and
specific blockers. Required checks should cover changed paths plus existing
upload/sync/checklist/report/share/browser and operator behavior as appropriate.
Use the repository's required lint/type/contract/test/build/export checks; do not
equate exports or simulator success with physical-device or live validation.

Write docs/progress/09-reliability-and-recovery.md, update implementation status and
relevant operational instructions, and prepare an evidence-based trial readiness
decision plus a short operator quickstart and prioritized remaining backlog. Fix
stale "PR #7 open" references in current status summaries without rewriting
historical evidence. Report local readiness, live readiness and independent
recovery separately. Every required acceptance gate must have reproducible PASS
evidence before Task 09 or the trial is declared complete; otherwise retain
implemented-awaiting-validation and list the exact continuation steps. Passing
local work may be committed and reviewed while physical/recovery gates remain open.

## Commit, push and open the Task 09 PR

1. Review changes and stage only relevant source, tests, migrations, runbooks and
   nonsecret evidence. Exclude .env, credentials, .local, tokens, screenshots with
   private content, originals, generated private PDFs, backups and signing material.
2. Commit coherently on the feature branch, never directly on main. Fetch main
   again and reconcile necessary changes safely.
   Preserve upstream CI/publication workflows; run affected checks after fixes or
   integration changes. Push the Task 09 feature branch without force push.
3. Create a new PR targeting main, or update an existing matching Task 09 PR. PR #7
   is already merged and must not be reused. Describe the actual reliability fixes,
   recovery/compatibility changes, validation scope and remaining release blockers.
4. Check CI for the latest pushed commit, repair failures attributable to this
   work, and report pending/unavailable checks honestly. Attach the PR to the
   current task with the available app artifact tool. Finish with branch, commit
   IDs, PR link, test/evidence summary, readiness verdict and precise next action.

If authentication or remote policy blocks publication, retain commits and report
the exact blocker; do not change credentials, permissions, visibility or paid plans
to bypass it. Do not merge or enable auto-merge. Do not deploy, activate automation,
message users or send invitations. Task 10 S3/Kafka/Temporal expansion stays deferred;
stop with Task 09's handoff and remaining trial gates rather than starting it.
